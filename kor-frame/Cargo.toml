[package]
name = "kor-frame"
version = "0.1.0"
edition = "2024"

[dependencies]
kor = { path = "../kor" }
buddy_system_allocator = { version = "0.11", default-features = false, features = ["alloc", "use_spin"] }
